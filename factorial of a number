#include <stdio.h>

int main()
{
   int f,n;
   printf("Enter the number\n");
   scanf("%d", &n);
   f=fact(n);
   printf("Factorial of %d id %d", n,f);
   return 0;
}
int fact(int n)
{
    if(n==1||n==0)
    return 1;
    else
    return n*fact(n-1);
}
